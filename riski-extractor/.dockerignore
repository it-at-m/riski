# Local .dockerignore for riski-extractor

# Note: Root-level .dockerignore handles most exclusions when build context is root

# Extractor-specific exclusions

artifacts/
src/docling/

# Keep this minimal since root .dockerignore handles the rest
