"""MCP tool definitions.

Placeholder scaffold — the tool functions (e.g. ``search_munich_ris``) land in
issue #04 and are wired into the Gradio MCP server in issue #06.
"""

# TODO(#04): define stateless, voice-friendly tool functions with full type hints
#            and Google-style docstrings (Gradio turns these into MCP tools):
#              - search_munich_ris(question: str) -> str   (primary)
#              - get_riski_capabilities() -> str           (optional)
#              - riski_health() -> str                     (optional)
